package com.example.agendaparaprofessores.ui.theme

import androidx.compose.ui.graphics.Color

/* ============================================================
 *  TOKENS NOVOS — identidade violeta
 *  Fundo lavanda + cards brancos + gradiente violeta.
 *  Só acrescenta: nenhuma cor antiga foi removida.
 * ============================================================ */

/* ---------- FUNDO ---------- */
val FundoLavanda     = Color(0xFFF2ECFF)   // fundo geral, lilás bem claro
val FundoLavandaTopo = Color(0xFFEDE3FF)   // topo do degradê de fundo
val FundoLavandaBase = Color(0xFFF8F5FF)   // base do degradê de fundo

/* ---------- SUPERFÍCIES ---------- */
val SuperficieBranca = Color(0xFFFFFFFF)   // cards
val SuperficieBaixa  = Color(0xFFF6F1FF)   // campos, fundos internos

/* ---------- VIOLETA ---------- */
val VioletaProfundo  = Color(0xFF4C1D95)   // início do gradiente / texto sobre claro
val Violeta          = Color(0xFF7C3AED)   // cor principal
val VioletaClaro     = Color(0xFFA855F7)   // fim do gradiente
val Magenta          = Color(0xFFC026D3)   // acento
val VioletaWash      = Color(0xFFF3EBFF)   // fundo de ícones/chips

/* ---------- TEXTO ---------- */
val TextoPrincipal   = Color(0xFF1A1523)
val TextoSecundario  = Color(0xFF6B6478)
val TextoSuave       = Color(0xFFA79FB3)

/* ---------- LINHAS ---------- */
val BordaSuave       = Color(0xFFEDE8F5)
val BordaForte       = Color(0xFFDCD1EE)

/* ============ LEGADO — template do Android Studio ============
 * Nenhum destes é usado no app. Pode apagar este bloco inteiro. */
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)
val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)
